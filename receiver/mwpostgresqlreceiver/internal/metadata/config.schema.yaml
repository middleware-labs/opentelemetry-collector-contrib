# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for postgresql metrics.
    type: object
    properties:
      postgresql.analyzed:
        description: "PostgresqlAnalyzedMetricConfig provides config for the postgresql.analyzed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.autoanalyzed:
        description: "PostgresqlAutoanalyzedMetricConfig provides config for the postgresql.autoanalyzed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.autovacuumed:
        description: "PostgresqlAutovacuumedMetricConfig provides config for the postgresql.autovacuumed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.backends:
        description: "PostgresqlBackendsMetricConfig provides config for the postgresql.backends metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.buffers.allocated:
        description: "PostgresqlBgwriterBuffersAllocatedMetricConfig provides config for the postgresql.bgwriter.buffers.allocated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.buffers.writes:
        description: "PostgresqlBgwriterBuffersWritesMetricConfig provides config for the postgresql.bgwriter.buffers.writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.checkpoint.count:
        description: "PostgresqlBgwriterCheckpointCountMetricConfig provides config for the postgresql.bgwriter.checkpoint.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.duration:
        description: "PostgresqlBgwriterDurationMetricConfig provides config for the postgresql.bgwriter.duration metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.bgwriter.maxwritten:
        description: "PostgresqlBgwriterMaxwrittenMetricConfig provides config for the postgresql.bgwriter.maxwritten metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.blk_read_time:
        description: "PostgresqlBlkReadTimeMetricConfig provides config for the postgresql.blk_read_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blk_write_time:
        description: "PostgresqlBlkWriteTimeMetricConfig provides config for the postgresql.blk_write_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blks_hit:
        description: "PostgresqlBlksHitMetricConfig provides config for the postgresql.blks_hit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blks_read:
        description: "PostgresqlBlksReadMetricConfig provides config for the postgresql.blks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.blocks_read:
        description: "PostgresqlBlocksReadMetricConfig provides config for the postgresql.blocks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.buffer_hit:
        description: "PostgresqlBufferHitMetricConfig provides config for the postgresql.buffer_hit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.commits:
        description: "PostgresqlCommitsMetricConfig provides config for the postgresql.commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.connection.count:
        description: "PostgresqlConnectionCountMetricConfig provides config for the postgresql.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.connection.max:
        description: "PostgresqlConnectionMaxMetricConfig provides config for the postgresql.connection.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.database.count:
        description: "PostgresqlDatabaseCountMetricConfig provides config for the postgresql.database.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.database.locks:
        description: "PostgresqlDatabaseLocksMetricConfig provides config for the postgresql.database.locks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.db_size:
        description: "PostgresqlDbSizeMetricConfig provides config for the postgresql.db_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.deadlocks:
        description: "PostgresqlDeadlocksMetricConfig provides config for the postgresql.deadlocks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.function.calls:
        description: "PostgresqlFunctionCallsMetricConfig provides config for the postgresql.function.calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.index.blocks_read:
        description: "PostgresqlIndexBlocksReadMetricConfig provides config for the postgresql.index.blocks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.index.rows_read:
        description: "PostgresqlIndexRowsReadMetricConfig provides config for the postgresql.index.rows_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.index.scans:
        description: "PostgresqlIndexScansMetricConfig provides config for the postgresql.index.scans metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.index.size:
        description: "PostgresqlIndexSizeMetricConfig provides config for the postgresql.index.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.index_bloat:
        description: "PostgresqlIndexBloatMetricConfig provides config for the postgresql.index_bloat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.live_rows:
        description: "PostgresqlLiveRowsMetricConfig provides config for the postgresql.live_rows metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.operations:
        description: "PostgresqlOperationsMetricConfig provides config for the postgresql.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.query.count:
        description: "PostgresqlQueryCountMetricConfig provides config for the postgresql.query.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.query.total_exec_time:
        description: "PostgresqlQueryTotalExecTimeMetricConfig provides config for the postgresql.query.total_exec_time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.replication.data_delay:
        description: "PostgresqlReplicationDataDelayMetricConfig provides config for the postgresql.replication.data_delay metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rollbacks:
        description: "PostgresqlRollbacksMetricConfig provides config for the postgresql.rollbacks metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows:
        description: "PostgresqlRowsMetricConfig provides config for the postgresql.rows metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows_deleted:
        description: "PostgresqlRowsDeletedMetricConfig provides config for the postgresql.rows_deleted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows_fetched:
        description: "PostgresqlRowsFetchedMetricConfig provides config for the postgresql.rows_fetched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows_inserted:
        description: "PostgresqlRowsInsertedMetricConfig provides config for the postgresql.rows_inserted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.rows_updated:
        description: "PostgresqlRowsUpdatedMetricConfig provides config for the postgresql.rows_updated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.sequential_scans:
        description: "PostgresqlSequentialScansMetricConfig provides config for the postgresql.sequential_scans metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.table.count:
        description: "PostgresqlTableCountMetricConfig provides config for the postgresql.table.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.table.size:
        description: "PostgresqlTableSizeMetricConfig provides config for the postgresql.table.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.table.vacuum.count:
        description: "PostgresqlTableVacuumCountMetricConfig provides config for the postgresql.table.vacuum.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.table_bloat:
        description: "PostgresqlTableBloatMetricConfig provides config for the postgresql.table_bloat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.temp.io:
        description: "PostgresqlTempIoMetricConfig provides config for the postgresql.temp.io metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.temp_files:
        description: "PostgresqlTempFilesMetricConfig provides config for the postgresql.temp_files metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.toast.blocks_hit:
        description: "PostgresqlToastBlocksHitMetricConfig provides config for the postgresql.toast.blocks_hit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.toast.index.blocks_read:
        description: "PostgresqlToastIndexBlocksReadMetricConfig provides config for the postgresql.toast.index.blocks_read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.toast.size:
        description: "PostgresqlToastSizeMetricConfig provides config for the postgresql.toast.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.transactions.duration.max:
        description: "PostgresqlTransactionsDurationMaxMetricConfig provides config for the postgresql.transactions.duration.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.transactions.duration.sum:
        description: "PostgresqlTransactionsDurationSumMetricConfig provides config for the postgresql.transactions.duration.sum metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.tup_deleted:
        description: "PostgresqlTupDeletedMetricConfig provides config for the postgresql.tup_deleted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_fetched:
        description: "PostgresqlTupFetchedMetricConfig provides config for the postgresql.tup_fetched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_inserted:
        description: "PostgresqlTupInsertedMetricConfig provides config for the postgresql.tup_inserted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_returned:
        description: "PostgresqlTupReturnedMetricConfig provides config for the postgresql.tup_returned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.tup_updated:
        description: "PostgresqlTupUpdatedMetricConfig provides config for the postgresql.tup_updated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.wal.age:
        description: "PostgresqlWalAgeMetricConfig provides config for the postgresql.wal.age metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.wal.count:
        description: "PostgresqlWalCountMetricConfig provides config for the postgresql.wal.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.wal.delay:
        description: "PostgresqlWalDelayMetricConfig provides config for the postgresql.wal.delay metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      postgresql.wal.lag:
        description: "PostgresqlWalLagMetricConfig provides config for the postgresql.wal.lag metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      postgresql.wal.size:
        description: "PostgresqlWalSizeMetricConfig provides config for the postgresql.wal.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  events_config:
    description: EventsConfig provides config for postgresql events.
    type: object
    properties:
      db.server.query_sample:
        description: EventConfig provides common config for a  db.server.query_sample event.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      db.server.top_query:
        description: EventConfig provides common config for a  db.server.top_query event.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for postgresql resource attributes.
    type: object
    properties:
      postgresql.database.name:
        description: ResourceAttributeConfig provides common config for a postgresql.database.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.db.version:
        description: ResourceAttributeConfig provides common config for a postgresql.db.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.index.name:
        description: ResourceAttributeConfig provides common config for a postgresql.index.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.schema.name:
        description: ResourceAttributeConfig provides common config for a postgresql.schema.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      postgresql.table.name:
        description: ResourceAttributeConfig provides common config for a postgresql.table.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_include:
            description: "Experimental: EventsInclude defines a list of filters for attribute values. If the list is not empty, only events with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          events_exclude:
            description: "Experimental: EventsExclude defines a list of filters for attribute values. If the list is not empty, events with matching resource attribute values will not be emitted. EventsInclude has higher priority than EventsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for postgresql metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
  logs_builder_config:
    description: LogsBuilderConfig is a configuration for postgresql logs builder.
    type: object
    properties:
      events:
        $ref: events_config
      resource_attributes:
        $ref: resource_attributes_config
