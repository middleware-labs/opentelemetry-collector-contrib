# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for mwmongodb metrics.
    type: object
    properties:
      mongodb.active.reads:
        description: "MongodbActiveReadsMetricConfig provides config for the mongodb.active.reads metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.active.writes:
        description: "MongodbActiveWritesMetricConfig provides config for the mongodb.active.writes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.asserts.msgps:
        description: "MongodbAssertsMsgpsMetricConfig provides config for the mongodb.asserts.msgps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.asserts.regularps:
        description: "MongodbAssertsRegularpsMetricConfig provides config for the mongodb.asserts.regularps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.asserts.rolloversps:
        description: "MongodbAssertsRolloverspsMetricConfig provides config for the mongodb.asserts.rolloversps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.asserts.userps:
        description: "MongodbAssertsUserpsMetricConfig provides config for the mongodb.asserts.userps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.asserts.warningps:
        description: "MongodbAssertsWarningpsMetricConfig provides config for the mongodb.asserts.warningps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.backgroundflushing.average_ms:
        description: "MongodbBackgroundflushingAverageMsMetricConfig provides config for the mongodb.backgroundflushing.average_ms metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.backgroundflushing.flushesps:
        description: "MongodbBackgroundflushingFlushespsMetricConfig provides config for the mongodb.backgroundflushing.flushesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.backgroundflushing.last_ms:
        description: "MongodbBackgroundflushingLastMsMetricConfig provides config for the mongodb.backgroundflushing.last_ms metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.backgroundflushing.total_ms:
        description: "MongodbBackgroundflushingTotalMsMetricConfig provides config for the mongodb.backgroundflushing.total_ms metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cache.operations:
        description: "MongodbCacheOperationsMetricConfig provides config for the mongodb.cache.operations metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.chunks.jumbo:
        description: "MongodbChunksJumboMetricConfig provides config for the mongodb.chunks.jumbo metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.chunks.total:
        description: "MongodbChunksTotalMetricConfig provides config for the mongodb.chunks.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.avgobjsize:
        description: "MongodbCollectionAvgobjsizeMetricConfig provides config for the mongodb.collection.avgobjsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.capped:
        description: "MongodbCollectionCappedMetricConfig provides config for the mongodb.collection.capped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.count:
        description: "MongodbCollectionCountMetricConfig provides config for the mongodb.collection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.indexsizes:
        description: "MongodbCollectionIndexsizesMetricConfig provides config for the mongodb.collection.indexsizes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.max:
        description: "MongodbCollectionMaxMetricConfig provides config for the mongodb.collection.max metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.maxsize:
        description: "MongodbCollectionMaxsizeMetricConfig provides config for the mongodb.collection.maxsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.nindexes:
        description: "MongodbCollectionNindexesMetricConfig provides config for the mongodb.collection.nindexes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.objects:
        description: "MongodbCollectionObjectsMetricConfig provides config for the mongodb.collection.objects metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.size:
        description: "MongodbCollectionSizeMetricConfig provides config for the mongodb.collection.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.collection.storagesize:
        description: "MongodbCollectionStoragesizeMetricConfig provides config for the mongodb.collection.storagesize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.commands.rate:
        description: "MongodbCommandsRateMetricConfig provides config for the mongodb.commands.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.connection.count:
        description: "MongodbConnectionCountMetricConfig provides config for the mongodb.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.numascopedconnections:
        description: "MongodbConnectionPoolNumascopedconnectionsMetricConfig provides config for the mongodb.connection_pool.numascopedconnections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.numclientconnections:
        description: "MongodbConnectionPoolNumclientconnectionsMetricConfig provides config for the mongodb.connection_pool.numclientconnections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.totalavailable:
        description: "MongodbConnectionPoolTotalavailableMetricConfig provides config for the mongodb.connection_pool.totalavailable metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.totalcreatedps:
        description: "MongodbConnectionPoolTotalcreatedpsMetricConfig provides config for the mongodb.connection_pool.totalcreatedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.totalinuse:
        description: "MongodbConnectionPoolTotalinuseMetricConfig provides config for the mongodb.connection_pool.totalinuse metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connection_pool.totalrefreshing:
        description: "MongodbConnectionPoolTotalrefreshingMetricConfig provides config for the mongodb.connection_pool.totalrefreshing metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.active:
        description: "MongodbConnectionsActiveMetricConfig provides config for the mongodb.connections.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.available:
        description: "MongodbConnectionsAvailableMetricConfig provides config for the mongodb.connections.available metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.awaitingtopologychanges:
        description: "MongodbConnectionsAwaitingtopologychangesMetricConfig provides config for the mongodb.connections.awaitingtopologychanges metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.current:
        description: "MongodbConnectionsCurrentMetricConfig provides config for the mongodb.connections.current metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.exhausthello:
        description: "MongodbConnectionsExhausthelloMetricConfig provides config for the mongodb.connections.exhausthello metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.exhaustismaster:
        description: "MongodbConnectionsExhaustismasterMetricConfig provides config for the mongodb.connections.exhaustismaster metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.loadbalanced:
        description: "MongodbConnectionsLoadbalancedMetricConfig provides config for the mongodb.connections.loadbalanced metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.rejected:
        description: "MongodbConnectionsRejectedMetricConfig provides config for the mongodb.connections.rejected metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.threaded:
        description: "MongodbConnectionsThreadedMetricConfig provides config for the mongodb.connections.threaded metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.connections.totalcreated:
        description: "MongodbConnectionsTotalcreatedMetricConfig provides config for the mongodb.connections.totalcreated metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursor.count:
        description: "MongodbCursorCountMetricConfig provides config for the mongodb.cursor.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursor.timeout.count:
        description: "MongodbCursorTimeoutCountMetricConfig provides config for the mongodb.cursor.timeout.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursors.timedout:
        description: "MongodbCursorsTimedoutMetricConfig provides config for the mongodb.cursors.timedout metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.cursors.totalopen:
        description: "MongodbCursorsTotalopenMetricConfig provides config for the mongodb.cursors.totalopen metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.data.size:
        description: "MongodbDataSizeMetricConfig provides config for the mongodb.data.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.database.count:
        description: "MongodbDatabaseCountMetricConfig provides config for the mongodb.database.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.deletes.rate:
        description: "MongodbDeletesRateMetricConfig provides config for the mongodb.deletes.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.document.operation.count:
        description: "MongodbDocumentOperationCountMetricConfig provides config for the mongodb.document.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.commits:
        description: "MongodbDurCommitsMetricConfig provides config for the mongodb.dur.commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.commitsinwritelock:
        description: "MongodbDurCommitsinwritelockMetricConfig provides config for the mongodb.dur.commitsinwritelock metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.compression:
        description: "MongodbDurCompressionMetricConfig provides config for the mongodb.dur.compression metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.earlycommits:
        description: "MongodbDurEarlycommitsMetricConfig provides config for the mongodb.dur.earlycommits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.journaledmb:
        description: "MongodbDurJournaledmbMetricConfig provides config for the mongodb.dur.journaledmb metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.commits:
        description: "MongodbDurTimemsCommitsMetricConfig provides config for the mongodb.dur.timems.commits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.commitsinwritelock:
        description: "MongodbDurTimemsCommitsinwritelockMetricConfig provides config for the mongodb.dur.timems.commitsinwritelock metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.dt:
        description: "MongodbDurTimemsDtMetricConfig provides config for the mongodb.dur.timems.dt metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.preplogbuffer:
        description: "MongodbDurTimemsPreplogbufferMetricConfig provides config for the mongodb.dur.timems.preplogbuffer metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.remapprivateview:
        description: "MongodbDurTimemsRemapprivateviewMetricConfig provides config for the mongodb.dur.timems.remapprivateview metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.writetodatafiles:
        description: "MongodbDurTimemsWritetodatafilesMetricConfig provides config for the mongodb.dur.timems.writetodatafiles metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.timems.writetojournal:
        description: "MongodbDurTimemsWritetojournalMetricConfig provides config for the mongodb.dur.timems.writetojournal metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.dur.writetodatafilesmb:
        description: "MongodbDurWritetodatafilesmbMetricConfig provides config for the mongodb.dur.writetodatafilesmb metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.extent.count:
        description: "MongodbExtentCountMetricConfig provides config for the mongodb.extent.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.extra_info.heap_usage_bytesps:
        description: "MongodbExtraInfoHeapUsageBytespsMetricConfig provides config for the mongodb.extra_info.heap_usage_bytesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.extra_info.page_faultsps:
        description: "MongodbExtraInfoPageFaultspsMetricConfig provides config for the mongodb.extra_info.page_faultsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.flushes.rate:
        description: "MongodbFlushesRateMetricConfig provides config for the mongodb.flushes.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.fsynclocked:
        description: "MongodbFsynclockedMetricConfig provides config for the mongodb.fsynclocked metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.getmores.rate:
        description: "MongodbGetmoresRateMetricConfig provides config for the mongodb.getmores.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.global_lock.time:
        description: "MongodbGlobalLockTimeMetricConfig provides config for the mongodb.global_lock.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.activeclients.readers:
        description: "MongodbGloballockActiveclientsReadersMetricConfig provides config for the mongodb.globallock.activeclients.readers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.activeclients.total:
        description: "MongodbGloballockActiveclientsTotalMetricConfig provides config for the mongodb.globallock.activeclients.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.activeclients.writers:
        description: "MongodbGloballockActiveclientsWritersMetricConfig provides config for the mongodb.globallock.activeclients.writers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.currentqueue.readers:
        description: "MongodbGloballockCurrentqueueReadersMetricConfig provides config for the mongodb.globallock.currentqueue.readers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.currentqueue.total:
        description: "MongodbGloballockCurrentqueueTotalMetricConfig provides config for the mongodb.globallock.currentqueue.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.currentqueue.writers:
        description: "MongodbGloballockCurrentqueueWritersMetricConfig provides config for the mongodb.globallock.currentqueue.writers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.locktime:
        description: "MongodbGloballockLocktimeMetricConfig provides config for the mongodb.globallock.locktime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.ratio:
        description: "MongodbGloballockRatioMetricConfig provides config for the mongodb.globallock.ratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.globallock.totaltime:
        description: "MongodbGloballockTotaltimeMetricConfig provides config for the mongodb.globallock.totaltime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.health:
        description: "MongodbHealthMetricConfig provides config for the mongodb.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.index.access.count:
        description: "MongodbIndexAccessCountMetricConfig provides config for the mongodb.index.access.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.index.count:
        description: "MongodbIndexCountMetricConfig provides config for the mongodb.index.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.index.size:
        description: "MongodbIndexSizeMetricConfig provides config for the mongodb.index.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.indexcounters.accessesps:
        description: "MongodbIndexcountersAccessespsMetricConfig provides config for the mongodb.indexcounters.accessesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.indexcounters.hitsps:
        description: "MongodbIndexcountersHitspsMetricConfig provides config for the mongodb.indexcounters.hitsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.indexcounters.missesps:
        description: "MongodbIndexcountersMissespsMetricConfig provides config for the mongodb.indexcounters.missesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.indexcounters.missratio:
        description: "MongodbIndexcountersMissratioMetricConfig provides config for the mongodb.indexcounters.missratio metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.indexcounters.resetsps:
        description: "MongodbIndexcountersResetspsMetricConfig provides config for the mongodb.indexcounters.resetsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.inserts.rate:
        description: "MongodbInsertsRateMetricConfig provides config for the mongodb.inserts.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.count:
        description: "MongodbLockAcquireCountMetricConfig provides config for the mongodb.lock.acquire.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.time:
        description: "MongodbLockAcquireTimeMetricConfig provides config for the mongodb.lock.acquire.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.acquire.wait_count:
        description: "MongodbLockAcquireWaitCountMetricConfig provides config for the mongodb.lock.acquire.wait_count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.lock.deadlock.count:
        description: "MongodbLockDeadlockCountMetricConfig provides config for the mongodb.lock.deadlock.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.locks.collection.acquirecount.exclusiveps:
        description: "MongodbLocksCollectionAcquirecountExclusivepsMetricConfig provides config for the mongodb.locks.collection.acquirecount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.acquirecount.intent_exclusiveps:
        description: "MongodbLocksCollectionAcquirecountIntentExclusivepsMetricConfig provides config for the mongodb.locks.collection.acquirecount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.acquirecount.intent_sharedps:
        description: "MongodbLocksCollectionAcquirecountIntentSharedpsMetricConfig provides config for the mongodb.locks.collection.acquirecount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.acquirecount.sharedps:
        description: "MongodbLocksCollectionAcquirecountSharedpsMetricConfig provides config for the mongodb.locks.collection.acquirecount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.acquirewaitcount.exclusiveps:
        description: "MongodbLocksCollectionAcquirewaitcountExclusivepsMetricConfig provides config for the mongodb.locks.collection.acquirewaitcount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.acquirewaitcount.sharedps:
        description: "MongodbLocksCollectionAcquirewaitcountSharedpsMetricConfig provides config for the mongodb.locks.collection.acquirewaitcount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.timeacquiringmicros.exclusiveps:
        description: "MongodbLocksCollectionTimeacquiringmicrosExclusivepsMetricConfig provides config for the mongodb.locks.collection.timeacquiringmicros.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.collection.timeacquiringmicros.sharedps:
        description: "MongodbLocksCollectionTimeacquiringmicrosSharedpsMetricConfig provides config for the mongodb.locks.collection.timeacquiringmicros.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirecount.exclusiveps:
        description: "MongodbLocksDatabaseAcquirecountExclusivepsMetricConfig provides config for the mongodb.locks.database.acquirecount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirecount.intent_exclusiveps:
        description: "MongodbLocksDatabaseAcquirecountIntentExclusivepsMetricConfig provides config for the mongodb.locks.database.acquirecount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirecount.intent_sharedps:
        description: "MongodbLocksDatabaseAcquirecountIntentSharedpsMetricConfig provides config for the mongodb.locks.database.acquirecount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirecount.sharedps:
        description: "MongodbLocksDatabaseAcquirecountSharedpsMetricConfig provides config for the mongodb.locks.database.acquirecount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirewaitcount.exclusiveps:
        description: "MongodbLocksDatabaseAcquirewaitcountExclusivepsMetricConfig provides config for the mongodb.locks.database.acquirewaitcount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirewaitcount.intent_exclusiveps:
        description: "MongodbLocksDatabaseAcquirewaitcountIntentExclusivepsMetricConfig provides config for the mongodb.locks.database.acquirewaitcount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirewaitcount.intent_sharedps:
        description: "MongodbLocksDatabaseAcquirewaitcountIntentSharedpsMetricConfig provides config for the mongodb.locks.database.acquirewaitcount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.acquirewaitcount.sharedps:
        description: "MongodbLocksDatabaseAcquirewaitcountSharedpsMetricConfig provides config for the mongodb.locks.database.acquirewaitcount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.timeacquiringmicros.exclusiveps:
        description: "MongodbLocksDatabaseTimeacquiringmicrosExclusivepsMetricConfig provides config for the mongodb.locks.database.timeacquiringmicros.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.timeacquiringmicros.intent_exclusiveps:
        description: "MongodbLocksDatabaseTimeacquiringmicrosIntentExclusivepsMetricConfig provides config for the mongodb.locks.database.timeacquiringmicros.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.timeacquiringmicros.intent_sharedps:
        description: "MongodbLocksDatabaseTimeacquiringmicrosIntentSharedpsMetricConfig provides config for the mongodb.locks.database.timeacquiringmicros.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.database.timeacquiringmicros.sharedps:
        description: "MongodbLocksDatabaseTimeacquiringmicrosSharedpsMetricConfig provides config for the mongodb.locks.database.timeacquiringmicros.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirecount.exclusiveps:
        description: "MongodbLocksGlobalAcquirecountExclusivepsMetricConfig provides config for the mongodb.locks.global.acquirecount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirecount.intent_exclusiveps:
        description: "MongodbLocksGlobalAcquirecountIntentExclusivepsMetricConfig provides config for the mongodb.locks.global.acquirecount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirecount.intent_sharedps:
        description: "MongodbLocksGlobalAcquirecountIntentSharedpsMetricConfig provides config for the mongodb.locks.global.acquirecount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirecount.sharedps:
        description: "MongodbLocksGlobalAcquirecountSharedpsMetricConfig provides config for the mongodb.locks.global.acquirecount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirewaitcount.exclusiveps:
        description: "MongodbLocksGlobalAcquirewaitcountExclusivepsMetricConfig provides config for the mongodb.locks.global.acquirewaitcount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirewaitcount.intent_exclusiveps:
        description: "MongodbLocksGlobalAcquirewaitcountIntentExclusivepsMetricConfig provides config for the mongodb.locks.global.acquirewaitcount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirewaitcount.intent_sharedps:
        description: "MongodbLocksGlobalAcquirewaitcountIntentSharedpsMetricConfig provides config for the mongodb.locks.global.acquirewaitcount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.acquirewaitcount.sharedps:
        description: "MongodbLocksGlobalAcquirewaitcountSharedpsMetricConfig provides config for the mongodb.locks.global.acquirewaitcount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.timeacquiringmicros.exclusiveps:
        description: "MongodbLocksGlobalTimeacquiringmicrosExclusivepsMetricConfig provides config for the mongodb.locks.global.timeacquiringmicros.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.timeacquiringmicros.intent_exclusiveps:
        description: "MongodbLocksGlobalTimeacquiringmicrosIntentExclusivepsMetricConfig provides config for the mongodb.locks.global.timeacquiringmicros.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.timeacquiringmicros.intent_sharedps:
        description: "MongodbLocksGlobalTimeacquiringmicrosIntentSharedpsMetricConfig provides config for the mongodb.locks.global.timeacquiringmicros.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.global.timeacquiringmicros.sharedps:
        description: "MongodbLocksGlobalTimeacquiringmicrosSharedpsMetricConfig provides config for the mongodb.locks.global.timeacquiringmicros.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.metadata.acquirecount.exclusiveps:
        description: "MongodbLocksMetadataAcquirecountExclusivepsMetricConfig provides config for the mongodb.locks.metadata.acquirecount.exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.metadata.acquirecount.sharedps:
        description: "MongodbLocksMetadataAcquirecountSharedpsMetricConfig provides config for the mongodb.locks.metadata.acquirecount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.acquirecount.intent_exclusiveps:
        description: "MongodbLocksMmapv1journalAcquirecountIntentExclusivepsMetricConfig provides config for the mongodb.locks.mmapv1journal.acquirecount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.acquirecount.intent_sharedps:
        description: "MongodbLocksMmapv1journalAcquirecountIntentSharedpsMetricConfig provides config for the mongodb.locks.mmapv1journal.acquirecount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.acquirewaitcount.intent_exclusiveps:
        description: "MongodbLocksMmapv1journalAcquirewaitcountIntentExclusivepsMetricConfig provides config for the mongodb.locks.mmapv1journal.acquirewaitcount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.acquirewaitcount.intent_sharedps:
        description: "MongodbLocksMmapv1journalAcquirewaitcountIntentSharedpsMetricConfig provides config for the mongodb.locks.mmapv1journal.acquirewaitcount.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.timeacquiringmicros.intent_exclusiveps:
        description: "MongodbLocksMmapv1journalTimeacquiringmicrosIntentExclusivepsMetricConfig provides config for the mongodb.locks.mmapv1journal.timeacquiringmicros.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.mmapv1journal.timeacquiringmicros.intent_sharedps:
        description: "MongodbLocksMmapv1journalTimeacquiringmicrosIntentSharedpsMetricConfig provides config for the mongodb.locks.mmapv1journal.timeacquiringmicros.intent_sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.acquirecount.intent_exclusiveps:
        description: "MongodbLocksOplogAcquirecountIntentExclusivepsMetricConfig provides config for the mongodb.locks.oplog.acquirecount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.acquirecount.sharedps:
        description: "MongodbLocksOplogAcquirecountSharedpsMetricConfig provides config for the mongodb.locks.oplog.acquirecount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.acquirewaitcount.intent_exclusiveps:
        description: "MongodbLocksOplogAcquirewaitcountIntentExclusivepsMetricConfig provides config for the mongodb.locks.oplog.acquirewaitcount.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.acquirewaitcount.sharedps:
        description: "MongodbLocksOplogAcquirewaitcountSharedpsMetricConfig provides config for the mongodb.locks.oplog.acquirewaitcount.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.timeacquiringmicros.intent_exclusiveps:
        description: "MongodbLocksOplogTimeacquiringmicrosIntentExclusivepsMetricConfig provides config for the mongodb.locks.oplog.timeacquiringmicros.intent_exclusiveps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.locks.oplog.timeacquiringmicros.sharedps:
        description: "MongodbLocksOplogTimeacquiringmicrosSharedpsMetricConfig provides config for the mongodb.locks.oplog.timeacquiringmicros.sharedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.mem.bits:
        description: "MongodbMemBitsMetricConfig provides config for the mongodb.mem.bits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.mem.mapped:
        description: "MongodbMemMappedMetricConfig provides config for the mongodb.mem.mapped metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.mem.mappedwithjournal:
        description: "MongodbMemMappedwithjournalMetricConfig provides config for the mongodb.mem.mappedwithjournal metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.mem.resident:
        description: "MongodbMemResidentMetricConfig provides config for the mongodb.mem.resident metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.mem.virtual:
        description: "MongodbMemVirtualMetricConfig provides config for the mongodb.mem.virtual metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.memory.usage:
        description: "MongodbMemoryUsageMetricConfig provides config for the mongodb.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.count.failedps:
        description: "MongodbMetricsCommandsCountFailedpsMetricConfig provides config for the mongodb.metrics.commands.count.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.count.total:
        description: "MongodbMetricsCommandsCountTotalMetricConfig provides config for the mongodb.metrics.commands.count.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.createindexes.failedps:
        description: "MongodbMetricsCommandsCreateindexesFailedpsMetricConfig provides config for the mongodb.metrics.commands.createindexes.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.createindexes.total:
        description: "MongodbMetricsCommandsCreateindexesTotalMetricConfig provides config for the mongodb.metrics.commands.createindexes.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.delete.failedps:
        description: "MongodbMetricsCommandsDeleteFailedpsMetricConfig provides config for the mongodb.metrics.commands.delete.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.delete.total:
        description: "MongodbMetricsCommandsDeleteTotalMetricConfig provides config for the mongodb.metrics.commands.delete.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.eval.failedps:
        description: "MongodbMetricsCommandsEvalFailedpsMetricConfig provides config for the mongodb.metrics.commands.eval.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.eval.total:
        description: "MongodbMetricsCommandsEvalTotalMetricConfig provides config for the mongodb.metrics.commands.eval.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.findandmodify.failedps:
        description: "MongodbMetricsCommandsFindandmodifyFailedpsMetricConfig provides config for the mongodb.metrics.commands.findandmodify.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.findandmodify.total:
        description: "MongodbMetricsCommandsFindandmodifyTotalMetricConfig provides config for the mongodb.metrics.commands.findandmodify.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.insert.failedps:
        description: "MongodbMetricsCommandsInsertFailedpsMetricConfig provides config for the mongodb.metrics.commands.insert.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.insert.total:
        description: "MongodbMetricsCommandsInsertTotalMetricConfig provides config for the mongodb.metrics.commands.insert.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.update.failedps:
        description: "MongodbMetricsCommandsUpdateFailedpsMetricConfig provides config for the mongodb.metrics.commands.update.failedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.commands.update.total:
        description: "MongodbMetricsCommandsUpdateTotalMetricConfig provides config for the mongodb.metrics.commands.update.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.cursor.open.notimeout:
        description: "MongodbMetricsCursorOpenNotimeoutMetricConfig provides config for the mongodb.metrics.cursor.open.notimeout metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.cursor.open.pinned:
        description: "MongodbMetricsCursorOpenPinnedMetricConfig provides config for the mongodb.metrics.cursor.open.pinned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.cursor.open.total:
        description: "MongodbMetricsCursorOpenTotalMetricConfig provides config for the mongodb.metrics.cursor.open.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.cursor.timedoutps:
        description: "MongodbMetricsCursorTimedoutpsMetricConfig provides config for the mongodb.metrics.cursor.timedoutps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.document.deletedps:
        description: "MongodbMetricsDocumentDeletedpsMetricConfig provides config for the mongodb.metrics.document.deletedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.document.insertedps:
        description: "MongodbMetricsDocumentInsertedpsMetricConfig provides config for the mongodb.metrics.document.insertedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.document.returnedps:
        description: "MongodbMetricsDocumentReturnedpsMetricConfig provides config for the mongodb.metrics.document.returnedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.document.updatedps:
        description: "MongodbMetricsDocumentUpdatedpsMetricConfig provides config for the mongodb.metrics.document.updatedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.getlasterror.wtime.numps:
        description: "MongodbMetricsGetlasterrorWtimeNumpsMetricConfig provides config for the mongodb.metrics.getlasterror.wtime.numps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.getlasterror.wtime.totalmillisps:
        description: "MongodbMetricsGetlasterrorWtimeTotalmillispsMetricConfig provides config for the mongodb.metrics.getlasterror.wtime.totalmillisps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.getlasterror.wtimeoutsps:
        description: "MongodbMetricsGetlasterrorWtimeoutspsMetricConfig provides config for the mongodb.metrics.getlasterror.wtimeoutsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.operation.fastmodps:
        description: "MongodbMetricsOperationFastmodpsMetricConfig provides config for the mongodb.metrics.operation.fastmodps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.operation.idhackps:
        description: "MongodbMetricsOperationIdhackpsMetricConfig provides config for the mongodb.metrics.operation.idhackps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.operation.scanandorderps:
        description: "MongodbMetricsOperationScanandorderpsMetricConfig provides config for the mongodb.metrics.operation.scanandorderps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.operation.writeconflictsps:
        description: "MongodbMetricsOperationWriteconflictspsMetricConfig provides config for the mongodb.metrics.operation.writeconflictsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.queryexecutor.scannedobjectsps:
        description: "MongodbMetricsQueryexecutorScannedobjectspsMetricConfig provides config for the mongodb.metrics.queryexecutor.scannedobjectsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.queryexecutor.scannedps:
        description: "MongodbMetricsQueryexecutorScannedpsMetricConfig provides config for the mongodb.metrics.queryexecutor.scannedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.record.movesps:
        description: "MongodbMetricsRecordMovespsMetricConfig provides config for the mongodb.metrics.record.movesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.apply.batches.numps:
        description: "MongodbMetricsReplApplyBatchesNumpsMetricConfig provides config for the mongodb.metrics.repl.apply.batches.numps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.apply.batches.totalmillisps:
        description: "MongodbMetricsReplApplyBatchesTotalmillispsMetricConfig provides config for the mongodb.metrics.repl.apply.batches.totalmillisps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.apply.opsps:
        description: "MongodbMetricsReplApplyOpspsMetricConfig provides config for the mongodb.metrics.repl.apply.opsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.buffer.count:
        description: "MongodbMetricsReplBufferCountMetricConfig provides config for the mongodb.metrics.repl.buffer.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.buffer.maxsizebytes:
        description: "MongodbMetricsReplBufferMaxsizebytesMetricConfig provides config for the mongodb.metrics.repl.buffer.maxsizebytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.buffer.sizebytes:
        description: "MongodbMetricsReplBufferSizebytesMetricConfig provides config for the mongodb.metrics.repl.buffer.sizebytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.network.bytesps:
        description: "MongodbMetricsReplNetworkBytespsMetricConfig provides config for the mongodb.metrics.repl.network.bytesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.network.getmores.numps:
        description: "MongodbMetricsReplNetworkGetmoresNumpsMetricConfig provides config for the mongodb.metrics.repl.network.getmores.numps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.network.getmores.totalmillisps:
        description: "MongodbMetricsReplNetworkGetmoresTotalmillispsMetricConfig provides config for the mongodb.metrics.repl.network.getmores.totalmillisps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.network.opsps:
        description: "MongodbMetricsReplNetworkOpspsMetricConfig provides config for the mongodb.metrics.repl.network.opsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.network.readerscreatedps:
        description: "MongodbMetricsReplNetworkReaderscreatedpsMetricConfig provides config for the mongodb.metrics.repl.network.readerscreatedps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.preload.docs.numps:
        description: "MongodbMetricsReplPreloadDocsNumpsMetricConfig provides config for the mongodb.metrics.repl.preload.docs.numps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.preload.docs.totalmillisps:
        description: "MongodbMetricsReplPreloadDocsTotalmillispsMetricConfig provides config for the mongodb.metrics.repl.preload.docs.totalmillisps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.preload.indexes.numps:
        description: "MongodbMetricsReplPreloadIndexesNumpsMetricConfig provides config for the mongodb.metrics.repl.preload.indexes.numps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.repl.preload.indexes.totalmillisps:
        description: "MongodbMetricsReplPreloadIndexesTotalmillispsMetricConfig provides config for the mongodb.metrics.repl.preload.indexes.totalmillisps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.ttl.deleteddocumentsps:
        description: "MongodbMetricsTTLDeleteddocumentspsMetricConfig provides config for the mongodb.metrics.ttl.deleteddocumentsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.metrics.ttl.passesps:
        description: "MongodbMetricsTTLPassespsMetricConfig provides config for the mongodb.metrics.ttl.passesps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.bytesinps:
        description: "MongodbNetworkBytesinpsMetricConfig provides config for the mongodb.network.bytesinps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.bytesoutps:
        description: "MongodbNetworkBytesoutpsMetricConfig provides config for the mongodb.network.bytesoutps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.io.receive:
        description: "MongodbNetworkIoReceiveMetricConfig provides config for the mongodb.network.io.receive metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.io.transmit:
        description: "MongodbNetworkIoTransmitMetricConfig provides config for the mongodb.network.io.transmit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.numrequestsps:
        description: "MongodbNetworkNumrequestspsMetricConfig provides config for the mongodb.network.numrequestsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.network.request.count:
        description: "MongodbNetworkRequestCountMetricConfig provides config for the mongodb.network.request.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.object.count:
        description: "MongodbObjectCountMetricConfig provides config for the mongodb.object.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.commandps:
        description: "MongodbOpcountersCommandpsMetricConfig provides config for the mongodb.opcounters.commandps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.deleteps:
        description: "MongodbOpcountersDeletepsMetricConfig provides config for the mongodb.opcounters.deleteps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.getmoreps:
        description: "MongodbOpcountersGetmorepsMetricConfig provides config for the mongodb.opcounters.getmoreps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.insertps:
        description: "MongodbOpcountersInsertpsMetricConfig provides config for the mongodb.opcounters.insertps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.queryps:
        description: "MongodbOpcountersQuerypsMetricConfig provides config for the mongodb.opcounters.queryps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcounters.updateps:
        description: "MongodbOpcountersUpdatepsMetricConfig provides config for the mongodb.opcounters.updateps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.commandps:
        description: "MongodbOpcountersreplCommandpsMetricConfig provides config for the mongodb.opcountersrepl.commandps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.deleteps:
        description: "MongodbOpcountersreplDeletepsMetricConfig provides config for the mongodb.opcountersrepl.deleteps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.getmoreps:
        description: "MongodbOpcountersreplGetmorepsMetricConfig provides config for the mongodb.opcountersrepl.getmoreps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.insertps:
        description: "MongodbOpcountersreplInsertpsMetricConfig provides config for the mongodb.opcountersrepl.insertps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.queryps:
        description: "MongodbOpcountersreplQuerypsMetricConfig provides config for the mongodb.opcountersrepl.queryps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.opcountersrepl.updateps:
        description: "MongodbOpcountersreplUpdatepsMetricConfig provides config for the mongodb.opcountersrepl.updateps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.operation.count:
        description: "MongodbOperationCountMetricConfig provides config for the mongodb.operation.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.operation.latency.time:
        description: "MongodbOperationLatencyTimeMetricConfig provides config for the mongodb.operation.latency.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.operation.repl.count:
        description: "MongodbOperationReplCountMetricConfig provides config for the mongodb.operation.repl.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.operation.time:
        description: "MongodbOperationTimeMetricConfig provides config for the mongodb.operation.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.commands.latency:
        description: "MongodbOplatenciesCommandsLatencyMetricConfig provides config for the mongodb.oplatencies.commands.latency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.commands.latencyps:
        description: "MongodbOplatenciesCommandsLatencypsMetricConfig provides config for the mongodb.oplatencies.commands.latencyps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.reads.latency:
        description: "MongodbOplatenciesReadsLatencyMetricConfig provides config for the mongodb.oplatencies.reads.latency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.reads.latencyps:
        description: "MongodbOplatenciesReadsLatencypsMetricConfig provides config for the mongodb.oplatencies.reads.latencyps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.writes.latency:
        description: "MongodbOplatenciesWritesLatencyMetricConfig provides config for the mongodb.oplatencies.writes.latency metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplatencies.writes.latencyps:
        description: "MongodbOplatenciesWritesLatencypsMetricConfig provides config for the mongodb.oplatencies.writes.latencyps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplog.logsizemb:
        description: "MongodbOplogLogsizembMetricConfig provides config for the mongodb.oplog.logsizemb metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplog.timediff:
        description: "MongodbOplogTimediffMetricConfig provides config for the mongodb.oplog.timediff metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.oplog.usedsizemb:
        description: "MongodbOplogUsedsizembMetricConfig provides config for the mongodb.oplog.usedsizemb metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.page_faults:
        description: "MongodbPageFaultsMetricConfig provides config for the mongodb.page_faults metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.profiling.level:
        description: "MongodbProfilingLevelMetricConfig provides config for the mongodb.profiling.level metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.profiling.slowms:
        description: "MongodbProfilingSlowmsMetricConfig provides config for the mongodb.profiling.slowms metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.queries.rate:
        description: "MongodbQueriesRateMetricConfig provides config for the mongodb.queries.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_commands_per_sec:
        description: "MongodbReplCommandsPerSecMetricConfig provides config for the mongodb.repl_commands_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_deletes_per_sec:
        description: "MongodbReplDeletesPerSecMetricConfig provides config for the mongodb.repl_deletes_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_getmores_per_sec:
        description: "MongodbReplGetmoresPerSecMetricConfig provides config for the mongodb.repl_getmores_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_inserts_per_sec:
        description: "MongodbReplInsertsPerSecMetricConfig provides config for the mongodb.repl_inserts_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_queries_per_sec:
        description: "MongodbReplQueriesPerSecMetricConfig provides config for the mongodb.repl_queries_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.repl_updates_per_sec:
        description: "MongodbReplUpdatesPerSecMetricConfig provides config for the mongodb.repl_updates_per_sec metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.replset.health:
        description: "MongodbReplsetHealthMetricConfig provides config for the mongodb.replset.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.replset.optime_lag:
        description: "MongodbReplsetOptimeLagMetricConfig provides config for the mongodb.replset.optime_lag metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.replset.replicationlag:
        description: "MongodbReplsetReplicationlagMetricConfig provides config for the mongodb.replset.replicationlag metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.replset.state:
        description: "MongodbReplsetStateMetricConfig provides config for the mongodb.replset.state metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.replset.votefraction:
        description: "MongodbReplsetVotefractionMetricConfig provides config for the mongodb.replset.votefraction metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.replset.votes:
        description: "MongodbReplsetVotesMetricConfig provides config for the mongodb.replset.votes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.session.count:
        description: "MongodbSessionCountMetricConfig provides config for the mongodb.session.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.cpu_nanos:
        description: "MongodbSlowOperationCPUNanosMetricConfig provides config for the mongodb.slow_operation.cpu_nanos metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.docs_examined:
        description: "MongodbSlowOperationDocsExaminedMetricConfig provides config for the mongodb.slow_operation.docs_examined metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.keys_examined:
        description: "MongodbSlowOperationKeysExaminedMetricConfig provides config for the mongodb.slow_operation.keys_examined metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.keys_inserted:
        description: "MongodbSlowOperationKeysInsertedMetricConfig provides config for the mongodb.slow_operation.keys_inserted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.ndeleted:
        description: "MongodbSlowOperationNdeletedMetricConfig provides config for the mongodb.slow_operation.ndeleted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.ninserted:
        description: "MongodbSlowOperationNinsertedMetricConfig provides config for the mongodb.slow_operation.ninserted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.nmatched:
        description: "MongodbSlowOperationNmatchedMetricConfig provides config for the mongodb.slow_operation.nmatched metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.nmodified:
        description: "MongodbSlowOperationNmodifiedMetricConfig provides config for the mongodb.slow_operation.nmodified metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.nreturned:
        description: "MongodbSlowOperationNreturnedMetricConfig provides config for the mongodb.slow_operation.nreturned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.num_yields:
        description: "MongodbSlowOperationNumYieldsMetricConfig provides config for the mongodb.slow_operation.num_yields metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.planning_time_micros:
        description: "MongodbSlowOperationPlanningTimeMicrosMetricConfig provides config for the mongodb.slow_operation.planning_time_micros metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.response_length:
        description: "MongodbSlowOperationResponseLengthMetricConfig provides config for the mongodb.slow_operation.response_length metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.time:
        description: "MongodbSlowOperationTimeMetricConfig provides config for the mongodb.slow_operation.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.slow_operation.write_conflicts:
        description: "MongodbSlowOperationWriteConflictsMetricConfig provides config for the mongodb.slow_operation.write_conflicts metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.avgobjsize:
        description: "MongodbStatsAvgobjsizeMetricConfig provides config for the mongodb.stats.avgobjsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.collections:
        description: "MongodbStatsCollectionsMetricConfig provides config for the mongodb.stats.collections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.datasize:
        description: "MongodbStatsDatasizeMetricConfig provides config for the mongodb.stats.datasize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.filesize:
        description: "MongodbStatsFilesizeMetricConfig provides config for the mongodb.stats.filesize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.indexes:
        description: "MongodbStatsIndexesMetricConfig provides config for the mongodb.stats.indexes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.indexsize:
        description: "MongodbStatsIndexsizeMetricConfig provides config for the mongodb.stats.indexsize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.numextents:
        description: "MongodbStatsNumextentsMetricConfig provides config for the mongodb.stats.numextents metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.objects:
        description: "MongodbStatsObjectsMetricConfig provides config for the mongodb.stats.objects metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.stats.storagesize:
        description: "MongodbStatsStoragesizeMetricConfig provides config for the mongodb.stats.storagesize metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.storage.size:
        description: "MongodbStorageSizeMetricConfig provides config for the mongodb.storage.size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.generic.current_allocated_bytes:
        description: "MongodbTcmallocGenericCurrentAllocatedBytesMetricConfig provides config for the mongodb.tcmalloc.generic.current_allocated_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.generic.heap_size:
        description: "MongodbTcmallocGenericHeapSizeMetricConfig provides config for the mongodb.tcmalloc.generic.heap_size metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.aggressive_memory_decommit:
        description: "MongodbTcmallocTcmallocAggressiveMemoryDecommitMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.aggressive_memory_decommit metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.central_cache_free_bytes:
        description: "MongodbTcmallocTcmallocCentralCacheFreeBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.central_cache_free_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.current_total_thread_cache_bytes:
        description: "MongodbTcmallocTcmallocCurrentTotalThreadCacheBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.current_total_thread_cache_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.max_total_thread_cache_bytes:
        description: "MongodbTcmallocTcmallocMaxTotalThreadCacheBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.max_total_thread_cache_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.pageheap_free_bytes:
        description: "MongodbTcmallocTcmallocPageheapFreeBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.pageheap_free_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.pageheap_unmapped_bytes:
        description: "MongodbTcmallocTcmallocPageheapUnmappedBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.pageheap_unmapped_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.spinlock_total_delay_ns:
        description: "MongodbTcmallocTcmallocSpinlockTotalDelayNsMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.spinlock_total_delay_ns metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.thread_cache_free_bytes:
        description: "MongodbTcmallocTcmallocThreadCacheFreeBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.thread_cache_free_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.tcmalloc.tcmalloc.transfer_cache_free_bytes:
        description: "MongodbTcmallocTcmallocTransferCacheFreeBytesMetricConfig provides config for the mongodb.tcmalloc.tcmalloc.transfer_cache_free_bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.updates.rate:
        description: "MongodbUpdatesRateMetricConfig provides config for the mongodb.updates.rate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.uptime:
        description: "MongodbUptimeMetricConfig provides config for the mongodb.uptime metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
      mongodb.usage.commands.count:
        description: "MongodbUsageCommandsCountMetricConfig provides config for the mongodb.usage.commands.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.commands.countps:
        description: "MongodbUsageCommandsCountpsMetricConfig provides config for the mongodb.usage.commands.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.commands.time:
        description: "MongodbUsageCommandsTimeMetricConfig provides config for the mongodb.usage.commands.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.getmore.count:
        description: "MongodbUsageGetmoreCountMetricConfig provides config for the mongodb.usage.getmore.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.getmore.countps:
        description: "MongodbUsageGetmoreCountpsMetricConfig provides config for the mongodb.usage.getmore.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.getmore.time:
        description: "MongodbUsageGetmoreTimeMetricConfig provides config for the mongodb.usage.getmore.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.insert.count:
        description: "MongodbUsageInsertCountMetricConfig provides config for the mongodb.usage.insert.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.insert.countps:
        description: "MongodbUsageInsertCountpsMetricConfig provides config for the mongodb.usage.insert.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.insert.time:
        description: "MongodbUsageInsertTimeMetricConfig provides config for the mongodb.usage.insert.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.queries.count:
        description: "MongodbUsageQueriesCountMetricConfig provides config for the mongodb.usage.queries.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.queries.countps:
        description: "MongodbUsageQueriesCountpsMetricConfig provides config for the mongodb.usage.queries.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.queries.time:
        description: "MongodbUsageQueriesTimeMetricConfig provides config for the mongodb.usage.queries.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.readlock.count:
        description: "MongodbUsageReadlockCountMetricConfig provides config for the mongodb.usage.readlock.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.readlock.countps:
        description: "MongodbUsageReadlockCountpsMetricConfig provides config for the mongodb.usage.readlock.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.readlock.time:
        description: "MongodbUsageReadlockTimeMetricConfig provides config for the mongodb.usage.readlock.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.remove.count:
        description: "MongodbUsageRemoveCountMetricConfig provides config for the mongodb.usage.remove.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.remove.countps:
        description: "MongodbUsageRemoveCountpsMetricConfig provides config for the mongodb.usage.remove.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.remove.time:
        description: "MongodbUsageRemoveTimeMetricConfig provides config for the mongodb.usage.remove.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.total.count:
        description: "MongodbUsageTotalCountMetricConfig provides config for the mongodb.usage.total.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.total.countps:
        description: "MongodbUsageTotalCountpsMetricConfig provides config for the mongodb.usage.total.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.total.time:
        description: "MongodbUsageTotalTimeMetricConfig provides config for the mongodb.usage.total.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.update.count:
        description: "MongodbUsageUpdateCountMetricConfig provides config for the mongodb.usage.update.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.update.countps:
        description: "MongodbUsageUpdateCountpsMetricConfig provides config for the mongodb.usage.update.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.update.time:
        description: "MongodbUsageUpdateTimeMetricConfig provides config for the mongodb.usage.update.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.writelock.count:
        description: "MongodbUsageWritelockCountMetricConfig provides config for the mongodb.usage.writelock.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.writelock.countps:
        description: "MongodbUsageWritelockCountpsMetricConfig provides config for the mongodb.usage.writelock.countps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.usage.writelock.time:
        description: "MongodbUsageWritelockTimeMetricConfig provides config for the mongodb.usage.writelock.time metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.bytes_currently_in_cache:
        description: "MongodbWiredtigerCacheBytesCurrentlyInCacheMetricConfig provides config for the mongodb.wiredtiger.cache.bytes_currently_in_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.failed_eviction_of_pages_exceeding_the_in_memory_maximumps:
        description: "MongodbWiredtigerCacheFailedEvictionOfPagesExceedingTheInMemoryMaximumpsMetricConfig provides config for the mongodb.wiredtiger.cache.failed_eviction_of_pages_exceeding_the_in_memory_maximumps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.in_memory_page_splits:
        description: "MongodbWiredtigerCacheInMemoryPageSplitsMetricConfig provides config for the mongodb.wiredtiger.cache.in_memory_page_splits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.maximum_bytes_configured:
        description: "MongodbWiredtigerCacheMaximumBytesConfiguredMetricConfig provides config for the mongodb.wiredtiger.cache.maximum_bytes_configured metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.maximum_page_size_at_eviction:
        description: "MongodbWiredtigerCacheMaximumPageSizeAtEvictionMetricConfig provides config for the mongodb.wiredtiger.cache.maximum_page_size_at_eviction metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.modified_pages_evicted:
        description: "MongodbWiredtigerCacheModifiedPagesEvictedMetricConfig provides config for the mongodb.wiredtiger.cache.modified_pages_evicted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.pages_currently_held_in_cache:
        description: "MongodbWiredtigerCachePagesCurrentlyHeldInCacheMetricConfig provides config for the mongodb.wiredtiger.cache.pages_currently_held_in_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.pages_evicted_by_application_threadsps:
        description: "MongodbWiredtigerCachePagesEvictedByApplicationThreadspsMetricConfig provides config for the mongodb.wiredtiger.cache.pages_evicted_by_application_threadsps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.pages_evicted_exceeding_the_in_memory_maximumps:
        description: "MongodbWiredtigerCachePagesEvictedExceedingTheInMemoryMaximumpsMetricConfig provides config for the mongodb.wiredtiger.cache.pages_evicted_exceeding_the_in_memory_maximumps metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.pages_read_into_cache:
        description: "MongodbWiredtigerCachePagesReadIntoCacheMetricConfig provides config for the mongodb.wiredtiger.cache.pages_read_into_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.pages_written_from_cache:
        description: "MongodbWiredtigerCachePagesWrittenFromCacheMetricConfig provides config for the mongodb.wiredtiger.cache.pages_written_from_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.tracked_dirty_bytes_in_cache:
        description: "MongodbWiredtigerCacheTrackedDirtyBytesInCacheMetricConfig provides config for the mongodb.wiredtiger.cache.tracked_dirty_bytes_in_cache metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.cache.unmodified_pages_evicted:
        description: "MongodbWiredtigerCacheUnmodifiedPagesEvictedMetricConfig provides config for the mongodb.wiredtiger.cache.unmodified_pages_evicted metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.read.available:
        description: "MongodbWiredtigerConcurrenttransactionsReadAvailableMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.read.available metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.read.out:
        description: "MongodbWiredtigerConcurrenttransactionsReadOutMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.read.out metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.read.totaltickets:
        description: "MongodbWiredtigerConcurrenttransactionsReadTotalticketsMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.read.totaltickets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.write.available:
        description: "MongodbWiredtigerConcurrenttransactionsWriteAvailableMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.write.available metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.write.out:
        description: "MongodbWiredtigerConcurrenttransactionsWriteOutMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.write.out metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wiredtiger.concurrenttransactions.write.totaltickets:
        description: "MongodbWiredtigerConcurrenttransactionsWriteTotalticketsMetricConfig provides config for the mongodb.wiredtiger.concurrenttransactions.write.totaltickets metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      mongodb.wtcache.bytes.read:
        description: "MongodbWtcacheBytesReadMetricConfig provides config for the mongodb.wtcache.bytes.read metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: false
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for mwmongodb resource attributes.
    type: object
    properties:
      database:
        description: ResourceAttributeConfig provides common config for a database resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      mongodb.database.name:
        description: ResourceAttributeConfig provides common config for a mongodb.database.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.address:
        description: ResourceAttributeConfig provides common config for a server.address resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.port:
        description: ResourceAttributeConfig provides common config for a server.port resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: false
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for mwmongodb metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
